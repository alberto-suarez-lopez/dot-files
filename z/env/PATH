# Z HOME ______________________________________________________________________
export Z_HOME="${HOME}/bin/z"

# PATH ________________________________________________________________________
[[ -z "${PATH}" ]] && { export PATH="${Z_HOME}"; } || \
    { export PATH="${Z_HOME}:${PATH}"; }
